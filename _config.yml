# GitHub Pages runs Jekyll unless .nojekyll is present.
# Keep agent manuals and build inputs in git; do not publish them.
# HTML without front matter is copied through as static files.
exclude:
  - AGENTS.md
  - CLAUDE.md
  - docs
  - tools
  - data
  - trucks
  - brand/telegram-qr.png
